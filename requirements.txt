# =============================================================================
# Genshin Impact Rich Presence - Current Dependencies
# Verified against embedded Python 3.13 environment
# =============================================================================

# Core dependencies (verified in embedded Python)
numpy==2.2.6
Pillow==11.3.0
pypresence==4.3.0
psutil==7.1.0

# OCR and image processing (verified)
easyocr==1.7.2
opencv-python-headless==4.12.0.88
scikit-image==0.25.2
scipy==1.16.2
imageio==2.37.0

# GUI dependencies (verified)
PyQt5==5.15.11

# Material 3 UI (Slint - verified)
slint==1.10.0

# Windows-specific (verified)
pywin32==311

# Utility libraries (verified)
requests==2.32.5
PyYAML==6.0.3
urllib3==2.5.0

# Additional verified dependencies
certifi==2025.10.5
charset-normalizer==3.4.3
filelock==3.20.0
idna==3.10
jinja2==3.1.6
lazy_loader==0.4
markupsafe==3.0.3
mpmath==1.3.0
networkx==3.5
ninja==1.13.0
pyclipper==1.3.0.post6
python-bidi==0.6.6
PyWavelets==1.9.0
shapely==2.1.2
sympy==1.13.1
tifffile==2025.10.4
torch==2.6.0+cu124
torchvision==0.21.0+cu124
typing_extensions==4.15.0
watchdog==6.0.0

# Note: This requirements.txt exactly matches the packages installed in the embedded Python environment.
# Used for both development and the embedded Python runtime to ensure consistency.
